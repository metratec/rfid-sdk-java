name=${project.name}
version=${project.version}
description=${project.description}
groupId=${project.groupId}
artifactId=${project.artifactId}
build.timestamp=${timestamp}
build.by=${user.name}
build.jdk=${java.runtime.version}
